package model;

/**
 * The Faculty enum represents the various faculties or schools within a
 * university.
 * Each enum value corresponds to a specific faculty, reflecting the diverse
 * academic
 * departments and disciplines available at the institution.
 */

public enum Faculty {
    SCHOOL,
    SCSE,
    NBS,
    ADM,
    SSS, EEE
}