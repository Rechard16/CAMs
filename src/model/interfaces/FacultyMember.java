package model.interfaces;

import model.Faculty;

/**
 * The FacultyMember interface is used to represent a faculty member.
 * It is used in the Suggestion class.
 */
public interface FacultyMember {
	public Faculty getFaculty();
}
